# Configuration for travis-ci testing with PostgreSQL database

dspace.dir = /opt/dryad/

db.name = postgres
db.url = jdbc:postgresql://localhost:5432/travis_ci_test

# JDBC Driver
db.driver = org.postgresql.Driver

# Database username and password
db.username = dryad_travis_user
db.password =

# 
# # Connection pool parameters
# 
# # Maximum number of DB connections in pool
# db.maxconnections = 300
# 
# # Maximum time to wait before giving up if all connections in pool are busy (milliseconds)
# db.maxwait = 10000
# 
# # Maximum number of idle connections in pool (-1 = unlimited)
# db.maxidle = -1
# 
# # Determine if prepared statement should be cached. (default is true)
# db.statementpool = false